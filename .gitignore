# OS
.DS_Store
Thumbs.db

# Editor
.idea
.vscode/*
!.vscode/extensions.json

# Env (e.g. a root .env used for `docker compose -f compose.prod.yaml` variable substitution)
.env
.env.local
.env.*.local

# Local-only docker compose overrides (e.g. exposing the database port to the host)
compose.override.yaml
